(jbuild_version 1)

(executables (
  (names (schur_complex schur_real))
  (flags (
    :standard
    -w -9
    -safe-string -strict-sequence
    -principal -short-paths
  ))
  (libraries (lacaml))
))
